
#include "libft.h"
#include <stdlib.h>
#include <stdio.h>

int		main(int argc, char** argv)
{
	char *str;

	if (argc != 2)
		{
			printf("%s. Type a number", argv[0]);
			return (0);
		}
	str = (char*)ft_memalloc(atoi(argv[1]));
	free(str);
	return (0);
}
